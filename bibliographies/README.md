# Bibliographies (per-project)

One file per project: `<project>-BIBLIOGRAPHY.md`. Cite the master
`../BIBLIOGRAPHY.md` by ID (`BIB-YYYY-NNN`) — no duplicated full citations.
Project-specific annotations live here.
